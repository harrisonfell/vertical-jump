import { ProgressScreen } from '@/features/progress';

/** /progress. The route stays thin; the screen lives in src/features/progress. */
export default function Progress() {
  return <ProgressScreen />;
}
